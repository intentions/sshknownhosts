ssh_key_harvester
Written by: Kurt J. Strosahl
Created 22may14

This is a two step process... first when a host is built its new key is placed in data/<hostname>
Then another process checks the data directory for new keys.  It then updates the ssh_known_hosts 
if a new key is detected.

