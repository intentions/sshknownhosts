ssh_key_harvester
Written by: Kurt J. Strosahl
Created 22may14

This process gathers host keys on server build, placing each host key in a file identified by hostname.
Then a process comes along and builds a ssh_known_host file from those files.
